# OnBeam Docs (Mintlify)

This folder is structured as a standalone Mintlify docs site.

## Run locally

Mintlify CLI requires Node 20+.

```bash
npx mintlify dev onbeam-docs
```

## Deploy

Connect your docs repository in Mintlify and set the docs directory to the repo root (where `mint.json` lives).

